import { BaseNavBarProps } from '../types';
export declare const BaseNavBar: ({ centerSlot, centerSlotClassName, className, leftSlot, leftSlotClassName, rightSlot, rightSlotClassName, isTransparent, }: BaseNavBarProps) => import("react/jsx-runtime").JSX.Element;
